scene:
  id: browser-demo
  name: Browser Demo
  mode: observe_only
  window_match:
    title_contains:
      - Demo
  capture_config:
    fps: 2
targets:
  - class_name: primary_button
    min_confidence: 0.6
    weight: 80
    cooldown_ms: 1000
